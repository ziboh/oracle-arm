# Optional Docker Compose overrides. Passwords, OCI credentials, task settings,
# and notification channels are configured in the browser.
BIND_ADDRESS=127.0.0.1
WEB_SECURE_COOKIE=false
# Optional: pin a published image version instead of :latest.
# ORACLE_ARM_IMAGE=ghcr.io/ziboh/oracle-arm:latest
